# Local PostgreSQL connection string used by Prisma.
# This matches docker-compose.yml and is for local development only.
DATABASE_URL="postgresql://conversational_mcq:conversational_mcq_dev_password@localhost:5432/conversational_mcq?schema=public"

# Use a long random value in real environments. Do not commit real secrets.
SESSION_SECRET="replace-with-at-least-32-random-characters"

# Deployment identity and URLs.
# APP_ENV must be local, staging, or production. In production, APP_BASE_URL must
# be the canonical public HTTPS origin, for example
# https://your-public-domain.example. Localhost is acceptable only for local
# development. NEXT_PUBLIC_APP_BASE_URL may mirror the public origin because it
# is harmless browser-visible configuration; it must never contain OpenAI,
# database, session, auth, cookie, or token secrets.
APP_ENV=local
APP_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_BASE_URL=http://localhost:3000

# Teacher/research account email recovery is intentionally disabled for the
# classroom pilot. Username remains the login identifier. The email-delivery
# variables below are deprecated placeholders and are not required for login,
# readiness, or the teacher Account settings UI. Do not commit real provider
# credentials.
EMAIL_PROVIDER=disabled
EMAIL_FROM=
EMAIL_REPLY_TO=
RESEND_API_KEY=
PASSWORD_RESET_TOKEN_TTL_MINUTES=30
EMAIL_CHANGE_TOKEN_TTL_MINUTES=30
TEACHER_PASSWORD_RESET_EMAIL_MAX_PER_HOUR=3
TEACHER_PASSWORD_RESET_IP_MAX_PER_HOUR=20
TEACHER_PASSWORD_RESET_GLOBAL_MAX_PER_HOUR=200
TEACHER_EMAIL_CHANGE_MAX_PER_HOUR=5
LIVE_TEACHER_EMAIL_SMOKE_RECIPIENT=

# Production/staging operator-only teacher username rename. Run only from the
# deployed service directory, for example Render Shell `/app`, with exact
# confirmation. The command updates the existing teacher row and preserves the
# existing password.
TEACHER_USERNAME_RENAME_ENABLED=false
CURRENT_TEACHER_USERNAME=
NEW_TEACHER_USERNAME=
CONFIRM_TEACHER_USERNAME_RENAME=

# Optional fresh-database classroom bootstrap teacher email. Email recovery is
# disabled; this value is retained only for older bootstrap metadata and should
# normally stay blank.
BOOTSTRAP_TEACHER_EMAIL=

# LLM provider configuration. Defaults keep the app fully local and mock-only.
LLM_PROVIDER=mock
LLM_LIVE_CALLS_ENABLED=false

# OpenAI placeholders. Leave blank for normal mock-mode development and smoke tests.
# Configure only when intentionally enabling server-side live OpenAI calls.
OPENAI_API_KEY=""
# Optional local secret file. Recommended local path:
# .data/secrets/openai_api_key
# The file is ignored by git via .data/ and should be owner-readable only.
OPENAI_API_KEY_FILE=""
OPENAI_MODEL_ITEM_VERIFICATION=""
OPENAI_MODEL_ITEM_ADMIN=""
OPENAI_MODEL_RESPONSE_COLLECTION=""
OPENAI_MODEL_PROFILING=""
OPENAI_MODEL_PROFILE_INTEGRATION=""
OPENAI_MODEL_PLANNING=""
OPENAI_MODEL_FOLLOWUP=""
OPENAI_MODEL_MCQ_DIAGNOSTIC_AUTHORING=""
OPENAI_MODEL_MCQ_FORMATTING=""
OPENAI_MODEL_CONNECTIVITY_TEST=""

# Advanced/testing only. Leave blank for paid OpenAI runs; the guarded-live
# transport gate approves only api.openai.com for paid transport.
OPENAI_BASE_URL=""

# Optional per-agent reasoning controls. Allowed values: none, low, medium,
# high, xhigh, max. Leave blank unless the selected model supports them.
OPENAI_REASONING_EFFORT_ITEM_VERIFICATION=""
OPENAI_REASONING_EFFORT_ITEM_ADMIN=""
OPENAI_REASONING_EFFORT_RESPONSE_COLLECTION=""
OPENAI_REASONING_EFFORT_PROFILING=""
OPENAI_REASONING_EFFORT_PROFILE_INTEGRATION=""
OPENAI_REASONING_EFFORT_PLANNING=""
OPENAI_REASONING_EFFORT_FOLLOWUP=""
OPENAI_REASONING_EFFORT_MCQ_DIAGNOSTIC_AUTHORING=""
OPENAI_REASONING_EFFORT_MCQ_FORMATTING=""
OPENAI_REASONING_EFFORT_CONNECTIVITY_TEST=""

# Candidate GPT-5.6 profile for explicit evaluation only. Do not activate
# without running the model-upgrade evaluation/approval workflow and updating
# OPERATIONAL_APPROVED_CONFIG_HASH.
# OPENAI_MODEL_ITEM_VERIFICATION=gpt-5.6-terra
# OPENAI_REASONING_EFFORT_ITEM_VERIFICATION=medium
# OPENAI_MODEL_ITEM_ADMIN=gpt-5.6-luna
# OPENAI_REASONING_EFFORT_ITEM_ADMIN=low
# OPENAI_MODEL_RESPONSE_COLLECTION=gpt-5.6-luna
# OPENAI_REASONING_EFFORT_RESPONSE_COLLECTION=low
# OPENAI_MODEL_PROFILING=gpt-5.6-terra
# OPENAI_REASONING_EFFORT_PROFILING=medium
# OPENAI_MODEL_PROFILE_INTEGRATION=gpt-5.6-terra
# OPENAI_REASONING_EFFORT_PROFILE_INTEGRATION=medium
# OPENAI_MODEL_PLANNING=gpt-5.6-sol
# OPENAI_REASONING_EFFORT_PLANNING=medium
# OPENAI_MODEL_FOLLOWUP=gpt-5.6-sol
# OPENAI_REASONING_EFFORT_FOLLOWUP=medium
# OPENAI_MODEL_MCQ_DIAGNOSTIC_AUTHORING=gpt-5.6-terra
# OPENAI_REASONING_EFFORT_MCQ_DIAGNOSTIC_AUTHORING=medium
# OPENAI_MODEL_MCQ_FORMATTING=gpt-5.6-luna
# OPENAI_REASONING_EFFORT_MCQ_FORMATTING=low
# OPENAI_MODEL_CONNECTIVITY_TEST=gpt-5.6-luna
# OPENAI_REASONING_EFFORT_CONNECTIVITY_TEST=none

OPENAI_MAX_OUTPUT_TOKENS_ITEM_VERIFICATION=""
OPENAI_MAX_OUTPUT_TOKENS_ITEM_ADMIN=""
OPENAI_MAX_OUTPUT_TOKENS_RESPONSE_COLLECTION=""
OPENAI_MAX_OUTPUT_TOKENS_PROFILING=""
OPENAI_MAX_OUTPUT_TOKENS_PROFILE_INTEGRATION=""
OPENAI_MAX_OUTPUT_TOKENS_PLANNING=""
OPENAI_MAX_OUTPUT_TOKENS_FOLLOWUP=""
OPENAI_MAX_OUTPUT_TOKENS_MCQ_DIAGNOSTIC_AUTHORING=""
OPENAI_MAX_OUTPUT_TOKENS_MCQ_FORMATTING=""

OPENAI_REQUEST_TIMEOUT_MS=60000
OPENAI_MAX_RETRIES=2

# Item Administration Tutor Agent runtime.
# auto = use live OpenAI when LLM_PROVIDER=openai, LLM_LIVE_CALLS_ENABLED=true,
# a server-side OpenAI credential is configured and authenticated, and
# OPENAI_MODEL_ITEM_ADMIN or OPENAI_MODEL_FOLLOWUP is set.
# If live config is missing in browser/runtime auto mode, the app blocks the
# open-text turn with a safe configuration message instead of silently using mock.
# Use mock only for tests, smoke commands, or intentional local mock walkthroughs.
# ALLOW_LOCAL_MOCK_RUNTIME is optional and defaults to false when unset; set it
# to exactly true only for an intentional local mock walkthrough.
# Store real local OpenAI keys in .env.local or OPENAI_API_KEY_FILE only; .env
# should not contain real OpenAI keys. Conflicting .env/.env.local key
# fingerprints fail closed.
# The older ITEM_ADMIN_TUTOR_LIVE_ENABLED flag is deprecated and no longer gates auto mode.
ITEM_ADMIN_TUTOR_MODE=auto
ITEM_ADMIN_TUTOR_LIVE_ENABLED=false
ALLOW_LOCAL_MOCK_RUNTIME=false

# One deployment instance serves one course. Store database timestamps in UTC,
# but display/parse assessment availability in this IANA course timezone.
COURSE_TIMEZONE=America/Edmonton

# Classroom workflow defaults. Keep these false for normal classroom use.
# Development smoke tests may temporarily override them server-side.
DEVELOPMENT_ACTIVE_SESSION_CONTROLS_ENABLED=false
ALLOW_MANUAL_REVIEW_STUDENT_STARTS=false

# Server-side operational LLM usage safeguards.
# These limits are not student-facing labels and are checked only on the backend.
LLM_DAILY_CLASS_CALL_LIMIT=200
LLM_DAILY_CLASS_TOKEN_LIMIT=500000
LLM_DAILY_STUDENT_CALL_LIMIT=25
LLM_DAILY_STUDENT_TOKEN_LIMIT=75000
LLM_SESSION_CALL_LIMIT=20
LLM_SESSION_TOKEN_LIMIT=50000
LLM_AGENT_CALL_LIMIT_PER_SESSION=8

# Cost limits remain optional until a versioned pricing registry is configured.
LLM_COST_WARNING_LIMIT_USD=""
LLM_COST_HARD_LIMIT_USD=""
LLM_USAGE_TIMEZONE=UTC

# Follow-up Agent context-window safeguards.
# These are technical limits for provider input and message size, not pedagogical turn caps.
FOLLOWUP_CONTEXT_MAX_TURNS=24
FOLLOWUP_MESSAGE_MAX_CHARS=6000
FOLLOWUP_CONTEXT_MAX_CHARS=50000
FOLLOWUP_SUBSTANTIVE_TURNS_BEFORE_UPDATE=3

# Phase 8A guarded operational agent integration.
# Keep disabled for ordinary local/classroom-style use. OPERATIONAL_AGENT_MODE is
# authoritative. The older OPERATIONAL_AGENT_INTEGRATION_ENABLED flag is a
# deprecated compatibility alias and must not conflict with the mode.
OPERATIONAL_AGENT_MODE=disabled
OPERATIONAL_APPROVED_CONFIG_HASH=
OPERATIONAL_EFFECTIVE_RESULT_VERSION=effective-system-eval-v2
OPERATIONAL_EFFECTIVE_VALIDATOR_VERSION=effective-validator-v1

# Phase 8B production-like synthetic E2E validation.
# These values are for isolated local testing only. The E2E database name must
# end with `_e2e`; never point this at the normal development database.
E2E_DATABASE_URL=postgresql://conversational_mcq:conversational_mcq_dev_password@localhost:5432/conversational_mcq_e2e?schema=public
E2E_BASE_URL=http://127.0.0.1:3100
E2E_FORBID_EXTERNAL_PROVIDER_CALLS=true
OPERATIONAL_AGENT_INTEGRATION_ENABLED=false
OPERATIONAL_AGENT_INTEGRATION_EVAL_EVIDENCE_REQUIRED=true
OPERATIONAL_AGENT_INTEGRATION_APPROVED_TARGETED_RUN_ID=evr_20260624_bltzgtq

# Phase 8C guarded-live synthetic operational canary.
# Paid execution is CLI-only, synthetic-only, and disabled by default. The
# canary database name must end with `_live_canary_e2e`.
OPERATIONAL_LIVE_CANARY_ENABLED=false
OPERATIONAL_LIVE_CANARY_TARGET_MODEL=gpt-5.4-mini-2026-03-17
OPERATIONAL_LIVE_CANARY_REASONING_EFFORT=low
OPERATIONAL_LIVE_CANARY_COST_HARD_LIMIT_USD=15
OPERATIONAL_LIVE_CANARY_MAX_PROVIDER_REQUESTS=80
OPERATIONAL_LIVE_CANARY_MAX_CONCURRENCY=1
OPERATIONAL_LIVE_CANARY_MAX_RETRIES=1
OPERATIONAL_LIVE_CANARY_REQUEST_TIMEOUT_MS=60000
OPERATIONAL_LIVE_CANARY_APPROVED_CONFIG_HASH=
OPERATIONAL_LIVE_CANARY_DATABASE_URL=postgresql://conversational_mcq:conversational_mcq_dev_password@localhost:5432/conversational_mcq_live_canary_e2e?schema=public
# Local loopback transport tests may set this internally; it must remain blank
# for any paid canary/probe.
OPERATIONAL_LIVE_CANARY_LOOPBACK_OPENAI_BASE_URL=

# Initial-administration free-text Response Collection Agent safeguards.
# Mock response-collection calls are disabled in ordinary student workflow unless this is explicitly true.
ALLOW_MOCK_RESPONSE_COLLECTION_IN_STUDENT_WORKFLOW=false
INITIAL_CHAT_MESSAGE_MAX_CHARS=6000
RESPONSE_COLLECTION_CONTEXT_MAX_TURNS=12
RESPONSE_COLLECTION_CONTEXT_MAX_CHARS=20000

# Async workflow job safeguards for automatic assessment mode.
WORKFLOW_JOB_MAX_ATTEMPTS=3
WORKFLOW_JOB_BASE_RETRY_MS=5000
WORKFLOW_JOB_MAX_RETRY_MS=300000
WORKFLOW_JOB_LEASE_TIMEOUT_MS=300000
WORKFLOW_JOB_POLL_INTERVAL_MS=2000

# Phase 7E2A internal evaluation harness defaults.
# Evaluation live calls are separate from classroom live calls. Keep disabled unless
# intentionally running the CLI-only paid canary after setting OPENAI_API_KEY locally.
EVAL_PROVIDER=mock
EVAL_LIVE_CALLS_ENABLED=false
EVAL_TARGET_MODEL=gpt-5.4-mini-2026-03-17
EVAL_REASONING_EFFORT=low
EVAL_DEFAULT_REPETITIONS=2
EVAL_CANARY_REPETITIONS=1
EVAL_CANARY_CASES_PER_AGENT=5
EVAL_COST_HARD_LIMIT_USD=50
EVAL_MAX_CONCURRENCY=1
EVAL_MAX_RETRIES=1
EVAL_REQUEST_TIMEOUT_MS=60000
EVAL_MAX_PROVIDER_REQUESTS=50
EVAL_MAX_OUTPUT_TOKENS_ITEM_VERIFICATION=3000
EVAL_MAX_OUTPUT_TOKENS_RESPONSE_COLLECTION=1500
EVAL_MAX_OUTPUT_TOKENS_PROFILING=4000
EVAL_MAX_OUTPUT_TOKENS_PLANNING=3000
EVAL_MAX_OUTPUT_TOKENS_FOLLOWUP=2500

# Phase 7E2B guarded full live evaluation pilot defaults.
# Pilot live calls are CLI-only and separate from classroom live calls.
# Leave disabled unless intentionally running the paid 100-output pilot locally.
EVAL_PILOT_PROVIDER=openai
EVAL_PILOT_LIVE_CALLS_ENABLED=false
EVAL_PILOT_APPROVED_CANARY_RUN_ID=
EVAL_PILOT_TARGET_MODEL=gpt-5.4-mini-2026-03-17
EVAL_PILOT_REASONING_EFFORT=low
EVAL_PILOT_REPETITIONS=2
EVAL_PILOT_INTERNAL_HOLDOUT_CASES_PER_AGENT=5
EVAL_PILOT_REPLICATION_CASES_PER_AGENT=5
EVAL_PILOT_COST_HARD_LIMIT_USD=50
EVAL_PILOT_MAX_PROVIDER_REQUESTS=150
EVAL_PILOT_MAX_CONCURRENCY=1
EVAL_PILOT_MAX_RETRIES=1
EVAL_PILOT_REQUEST_TIMEOUT_MS=60000

# Phase 7E2C targeted remediation regression defaults.
# Uses the same eval provider enablement and exact model snapshot as live eval,
# but has its own smaller budget and request guard.
EVAL_TARGETED_REMEDIATION_COST_HARD_LIMIT_USD=10
EVAL_TARGETED_REMEDIATION_MAX_PROVIDER_REQUESTS=35
EVAL_TARGETED_REMEDIATION_MAX_CONCURRENCY=1
EVAL_TARGETED_REMEDIATION_MAX_RETRIES=1
EVAL_TARGETED_REMEDIATION_REQUEST_TIMEOUT_MS=60000

# Student browser process-event thresholds for local development.
# These are technical debounce/timing defaults, not psychological thresholds.
NEXT_PUBLIC_LONG_PAUSE_MS=120000
NEXT_PUBLIC_INACTIVITY_MS=300000

# Teacher-authored item media storage. Uploads remain disabled unless all S3-compatible
# settings are provided server-side. Do not commit real storage credentials.
MEDIA_STORAGE_PROVIDER=
MEDIA_STORAGE_BUCKET=
MEDIA_STORAGE_REGION=
MEDIA_STORAGE_ENDPOINT=
MEDIA_STORAGE_ACCESS_KEY_ID=
MEDIA_STORAGE_SECRET_ACCESS_KEY=
MEDIA_STORAGE_PUBLIC_BASE_URL=
MEDIA_VIDEO_EMBED_ALLOWLIST=

# Phase 31d first-run staging bootstrap. Set these only for the explicit
# `npm run staging:bootstrap-pilot` operator command after migrations complete.
# Do not commit real teacher passwords or generated access-code outputs.
BOOTSTRAP_ENABLED=false
BOOTSTRAP_TEACHER_USERNAME=
BOOTSTRAP_TEACHER_PASSWORD=
BOOTSTRAP_CLASSROOM_ID=
BOOTSTRAP_CLASSROOM_NAME=
BOOTSTRAP_STUDENT_COUNT=
BOOTSTRAP_STUDENT_ROSTER_PATH=
BOOTSTRAP_DEFAULT_ASSESSMENT_ID=assessment_mvp_irt_theta_invariance

# Research export pseudonymization. Production research exports require a
# server-side HMAC key; do not commit real key material. Development/tests use a
# deterministic non-production test key when this is unset.
RESEARCH_PSEUDONYMIZATION_KEY=
RESEARCH_PSEUDONYMIZATION_VERSION=hmac_sha256_v1
